import AppShell from '../shared/AppShell'

export default function Briefing() {
  return (
    <AppShell>
      <div style={{ padding: '0 40px', flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <span style={{ fontSize: 14, color: 'var(--tx3)' }}>Briefing — Coming Soon</span>
      </div>
    </AppShell>
  )
}
